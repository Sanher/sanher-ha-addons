# Sanher HA Add-ons

Repositorio de add-ons personalizados para Home Assistant.

## Añadir repositorio

En Home Assistant:

1. `Settings -> Add-ons -> Add-on Store`
2. Menu de tres puntos -> `Repositories`
3. Añadir: `https://github.com/sanher/sanher-ha-addons`

## Add-ons disponibles

- `17Track App`: servidor Node para gestion de paquetes y refresco en background.

- `Agent Runner`: runner de automatizaciones IA/web con API y persistencia en /data.

- `Learn Languages`: AI-assisted Japanese learning add-on with daily lessons, adaptive practice games, progress tracking, and a web UI through Home Assistant ingress.

- `Happy Server`: despliegue autoalojado de Happy Server con PostgreSQL y Redis internos en el mismo contenedor.

- `RustDesk Server`: servidor RustDesk OSS minimalista para Home Assistant, sin ingress y con persistencia en /data.
